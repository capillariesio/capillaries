# https://golangci-lint.run/usage/configuration/#config-file
# ~/go/bin/golangci-lint run ./pkg/...
# gofmt -s -w ./pkg

version: "2"
formatters:
  enable:
    - goimports

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - revive
    - govet
    - forbidigo
  settings:  
    revive:
      severity: error
      confidence: 0.8
      enable-all-rules: true
      rules:
        # Disabled rules
        - name: confusing-results
          disabled: true
        - name: add-constant
          disabled: true
        - name: argument-limit
          disabled: true
        - name: empty-lines
          disabled: true
        - name: exported
          disabled: true
        - name: function-length
          disabled: true
        - name: line-length-limit
          disabled: true
        - name: max-public-structs
          disabled: true
        - name: unused-receiver
          disabled: true
        - name: var-naming
          disabled: true
        - name: flag-parameter
          disabled: true

        # Rule tuning
        - name: max-control-nesting
          arguments:
            - 8
        - name: cognitive-complexity
          arguments:
            - 50
        - name: cyclomatic
          arguments:
            - 30
        - name: function-result-limit
          arguments:
            - 6
        - name: unhandled-error
          arguments:
            - "fmt.*"
            - "bytes.Buffer.*"
            - "strings.Builder.*"
            - "os.File.Close"
            - "io.Closer.Close"
            - "zap.Logger.Sync"
            - "net.Conn.Close"


  exclusions:
    warn-unused: true
    presets:
      - std-error-handling
      - common-false-positives
    rules:

      - path: pkg\/xfer\/ssh\.go
        text: "SA1019: x509.IsEncryptedPEMBlock|SA1019: x509.DecryptPEMBlock"
        linters: [ staticcheck ]

      - path: .+\.go
        text: Error return value of .((os\.)?std(out|err)\..*|.*Close|.*Sync|.*Flush|os\.Remove(All)?|.*print(f|ln)?|os\.(Un)?Setenv). is not checked
        linters: [ errcheck ]

      - path: pkg\/exe\/toolbelt\/.+\.go
        text: use of `fmt.Print(|f|ln)` forbidden by pattern
        linters: [ forbidigo]
      - path: pkg\/capigraph\/svg_test\.go
        text: use of `fmt.Print(|f|ln)` forbidden by pattern
        linters: [ forbidigo]

      - path: .+\.go
        text: struct literal uses unkeyed fields
        linters: [ govet ]

      - path: .+\.go
        text: replace sort.Slice by slices.SortFunc
        linters: [ revive ]

      - path: pkg\/gocqlmem\/parser\.go
        text: has (cognitive|cyclomatic) complexity
        linters: [ revive ]
      - path: pkg\/gocqlmem\/table\.go
        text: has (cognitive|cyclomatic) complexity
        linters: [ revive ]
      - path: pkg\/gocqlmem\/type_helpers\.go
        text: function compareInternalInExpressions has cyclomatic complexity
        linters: [ revive ]
      - path: pkg\/gocqlmem\/type_helpers\.go
        text: function compareInternalKnownType has cyclomatic complexity
        linters: [ revive ]
      - path: pkg\/gocqlmem\/type_helpers\.go
        text: function clientTypedValueToProvidedPtr has cyclomatic complexity
        linters: [ revive ]
      - path: pkg\/proc\/proc_table_creator\.go
        text: function RunCreateTableRelForBatch has (cyclomatic|cognitive) complexity
        linters: [ revive ]
      - path: pkg\/capigraph\/layering\.go
        text: function buildLayerMap has cognitive complexity
        linters: [ revive ]
      - path: pkg\/api\/capigraph\.go
        text: function GetCapigraphDiagram has (cognitive|cyclomatic) complexity
        linters: [ revive ]
      - path: pkg\/eval\/eval_ctx\.go
        text: function \(\*EvalCtx\)\.Eval has (cognitive|cyclomatic) complexity
        linters: [ revive ]
      - path: pkg\/gocqlmem\/eval_gocqlmem_functions\.go
        text: function callCast has cyclomatic complexity
        linters: [ revive ]
      - path: pkg\/proc\/proc_file_creator\.go
        text: "unchecked-type-assertion: type cast result is unchecked in [^ ]+FileRecordHeapItem"
        linters: [ revive ]
      - path: pkg\/api\/message_handler\.go
        text: function ProcessDataBatchMsg has cyclomatic complexity
        linters: [ revive ]

        
